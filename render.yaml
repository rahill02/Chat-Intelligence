services:
  - type: web
    name: chat-intelligence
    runtime: docker
    plan: free
    region: oregon
    envVars:
      - key: ENVIRONMENT
        value: production
      - key: LLM_PROVIDER
        value: mock
    healthCheckPath: /api/health